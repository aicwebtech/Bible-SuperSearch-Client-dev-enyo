var kind = require('enyo/kind');
var Base = require('../FormatButtonsBase')

module.exports = kind({
    name: 'FormatButtons',
    kind: Base,

    components: [
        {content: 'format buttons twentytwenty'}
    ]
});
