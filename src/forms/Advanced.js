var kind = require('enyo/kind');
var FormBase = require('./FormBase');

module.exports = kind({
    name: 'Advanced',
    kind: FormBase,

    components: [
        {content: 'Advanced FORM'}
    ]
});
