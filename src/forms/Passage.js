var kind = require('enyo/kind');
var FormBase = require('./FormBase');

module.exports = kind({
    name: 'Passage',
    kind: FormBase,

    components: [
        {content: 'Passage FORM'}
    ]
});
