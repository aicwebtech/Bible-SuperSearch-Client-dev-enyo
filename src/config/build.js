var buildConfig = {
    "name": "standalone",
    "dynamicConfig": "false",  // Indicates we are loading a config from a database through an ajax call
    "dynamicConfigUrl": ""  // URL to the dynamic config
};

module.exports = buildConfig;