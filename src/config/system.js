var systemConfig = {
    'rootDir': '/biblesupersearch'
};

systemConfig.rootDir = (typeof biblesupersearch_root_directory == 'string') ? biblesupersearch_root_directory : '/biblesupersearch';

module.exports = systemConfig;